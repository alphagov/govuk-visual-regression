require "rspec/core/rake_task"

lib = File.expand_path('../lib', __FILE__)
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)
load File.dirname(__FILE__) + "/lib/govuk_visual_regression/pages/tasks/rakefile.rake"
